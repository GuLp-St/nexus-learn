/** Nexon cost to generate a full course (upload or AI topic). */
export const COURSE_GENERATION_NEXON_COST = 150

/** Minimum final quiz score (%) required to publish a course. */
export const PUBLISH_MIN_QUIZ_SCORE = 70

/** Nexon cost to publish a course to the community library. */
export const PUBLISH_NEXON_COST = 500

/** Nexon cost to push updates to an already-published course. */
export const REPUBLISH_NEXON_COST = 250

/** Minimum account level to publish. */
export const PUBLISH_MIN_LEVEL = 5
